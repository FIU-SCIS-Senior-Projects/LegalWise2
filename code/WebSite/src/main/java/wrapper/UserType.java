package wrapper;

/**
 * Serves as a profile for each user. 
 * Each value in this enum will be linked to a set
 * of permissions that will be granted to users.
 * @author Fernando
 *
 */
public enum UserType {
	GUEST,
	REGULAR,
	ADMIN,
	SUPER
}
